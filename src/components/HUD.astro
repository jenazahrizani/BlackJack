<!--
  BLACKJACK 21 — AMBIENT HUD

  PURPOSE
  --------------------------------------------------------------------------
  This component is intentionally decorative / informational only.

  Game authority:
    Blackjack.ts

  Game integration:
    Game.ts

  Interactive controls:
    Game.astro

  Rendering:
    Renderer.ts

  DO NOT put gameplay logic here.

  This overlay:
    - does not own buttons
    - does not own bets
    - does not own cards
    - does not mutate game state
    - does not listen for gameplay actions
    - does not create a second input system

  It exists only to make the casino world feel like a real private table.
-->

<div
  class="ambient-hud"
  aria-hidden="true"
>
  <!-- =========================================================
       TOP LEFT — TABLE IDENTIFICATION
       ========================================================= -->

  <div class="ambient-mark ambient-mark--left">
    <span class="ambient-mark__line"></span>

    <div class="ambient-mark__copy">
      <small>TABLE</small>
      <strong>07</strong>
    </div>
  </div>


  <!-- =========================================================
       TOP RIGHT — SHOE IDENTIFICATION
       ========================================================= -->

  <div class="ambient-mark ambient-mark--right">
    <div class="ambient-mark__copy">
      <small>SHOE</small>
      <strong>06</strong>
    </div>

    <span class="ambient-mark__line"></span>
  </div>


  <!-- =========================================================
       TOP CENTER — HOUSE MARK
       ========================================================= -->

  <div class="ambient-title">
    <span>BLACKJACK</span>

    <i></i>

    <strong>21</strong>
  </div>


  <!-- =========================================================
       BOTTOM ENVIRONMENTAL RULE
       ========================================================= -->

  <div class="table-rules">
    <span>BLACKJACK 3 : 2</span>

    <i></i>

    <span>DEALER STANDS 17</span>

    <i></i>

    <span>MIN $5</span>
  </div>


  <!-- =========================================================
       ATMOSPHERIC DEPTH
       ========================================================= -->

  <div
    class="hud-vignette"
    aria-hidden="true"
  ></div>


  <div
    class="hud-grain"
    aria-hidden="true"
  ></div>
</div>


<style>
  /* ==========================================================
     ROOT
     ========================================================== */

  .ambient-hud {
    position:
      absolute;

    inset:
      0;

    z-index:
      20;

    width:
      100%;

    height:
      100%;

    overflow:
      hidden;

    pointer-events:
      none;

    color:
      #e7e1d3;

    font-family:
      "Courier New",
      Courier,
      monospace;

    user-select:
      none;

    /*
     * Do not allow the overlay to create a new click/input layer.
     */
    touch-action:
      none;

    /*
     * Keep the browser from trying to smooth the tiny lettering.
     */
    image-rendering:
      pixelated;

    -webkit-font-smoothing:
      none;
  }


  /* ==========================================================
     TOP AMBIENT MARKS
     ========================================================== */

  .ambient-mark {
    position:
      absolute;

    top:
      18px;

    display:
      flex;

    align-items:
      center;

    gap:
      7px;

    opacity:
      0.38;

    line-height:
      1;

    text-shadow:
      1px 1px 0
      rgba(
        0,
        0,
        0,
        0.82
      );
  }


  .ambient-mark--left {
    left:
      18px;
  }


  .ambient-mark--right {
    right:
      18px;
  }


  /* ==========================================================
     SMALL LINE
     ========================================================== */

  .ambient-mark__line {
    width:
      16px;

    height:
      1px;

    flex:
      0 0 auto;

    background:
      rgba(
        197,
        163,
        92,
        0.42
      );
  }


  /* ==========================================================
     AMBIENT COPY
     ========================================================== */

  .ambient-mark__copy {
    display:
      flex;

    align-items:
      baseline;

    gap:
      5px;

    line-height:
      1;
  }


  .ambient-mark__copy small {
    margin:
      0;

    font-size:
      5px;

    font-weight:
      700;

    letter-spacing:
      1.4px;

    color:
      rgba(
        231,
        225,
        211,
        0.30
      );
  }


  .ambient-mark__copy strong {
    margin:
      0;

    font-size:
      7px;

    font-weight:
      700;

    letter-spacing:
      1px;

    color:
      rgba(
        197,
        163,
        92,
        0.66
      );
  }


  /* ==========================================================
     CENTER HOUSE MARK
     ========================================================== */

  .ambient-title {
    position:
      absolute;

    top:
      18px;

    left:
      50%;

    transform:
      translateX(-50%);

    display:
      flex;

    align-items:
      center;

    gap:
      7px;

    opacity:
      0.26;

    line-height:
      1;

    white-space:
      nowrap;

    text-shadow:
      1px 1px 0
      rgba(
        0,
        0,
        0,
        0.82
      );
  }


  .ambient-title span {
    font-size:
      5px;

    font-weight:
      700;

    letter-spacing:
      2px;

    color:
      rgba(
        197,
        163,
        92,
        0.56
      );
  }


  .ambient-title strong {
    font-size:
      7px;

    font-weight:
      700;

    letter-spacing:
      1px;

    color:
      rgba(
        231,
        225,
        211,
        0.48
      );
  }


  .ambient-title i {
    width:
      2px;

    height:
      2px;

    flex:
      0 0 auto;

    display:
      block;

    background:
      rgba(
        197,
        163,
        92,
        0.52
      );
  }


  /* ==========================================================
     TABLE RULES
     ========================================================== */

  .table-rules {
    position:
      absolute;

    left:
      50%;

    bottom:
      17px;

    transform:
      translateX(-50%);

    display:
      flex;

    align-items:
      center;

    justify-content:
      center;

    gap:
      8px;

    max-width:
      calc(
        100vw - 24px
      );

    white-space:
      nowrap;

    opacity:
      0.27;

    line-height:
      1;

    font-size:
      5px;

    font-weight:
      700;

    letter-spacing:
      1.1px;

    color:
      rgba(
        231,
        225,
        211,
        0.42
      );

    text-shadow:
      1px 1px 0
      rgba(
        0,
        0,
        0,
        0.78
      );
  }


  .table-rules i {
    width:
      2px;

    height:
      2px;

    flex:
      0 0 auto;

    display:
      block;

    background:
      rgba(
        197,
        163,
        92,
        0.48
      );
  }


  .table-rules span:first-child {
    color:
      rgba(
        197,
        163,
        92,
        0.54
      );
  }


  /* ==========================================================
     AMBIENT VIGNETTE
     
     Kept extremely weak because Renderer.ts already supplies
     the primary cinematic vignette.
     ========================================================== */

  .hud-vignette {
    position:
      absolute;

    inset:
      0;

    pointer-events:
      none;

    background:
      radial-gradient(
        ellipse at center 48%,
        rgba(
          0,
          0,
          0,
          0
        )
        50%,
        rgba(
          0,
          0,
          0,
          0.018
        )
        67%,
        rgba(
          0,
          0,
          0,
          0.075
        )
        84%,
        rgba(
          0,
          0,
          0,
          0.16
        )
        100%
      );
  }


  /* ==========================================================
     PIXEL GRAIN
     
     Very subtle. Renderer already handles the main pixel treatment.
     ========================================================== */

  .hud-grain {
    position:
      absolute;

    inset:
      0;

    pointer-events:
      none;

    opacity:
      0.028;

    background-image:
      repeating-linear-gradient(
        0deg,
        rgba(
          255,
          255,
          255,
          0.025
        )
        0,
        rgba(
          255,
          255,
          255,
          0.025
        )
        1px,
        transparent 1px,
        transparent 5px
      );
  }


  /* ==========================================================
     MEDIUM SCREENS
     ========================================================== */

  @media (
    max-width: 800px
  ) {
    .ambient-mark {
      top:
        13px;
    }


    .ambient-mark--left {
      left:
        13px;
    }


    .ambient-mark--right {
      right:
        13px;
    }


    .ambient-title {
      top:
        13px;
    }


    .ambient-mark__copy {
      gap:
        4px;
    }


    .ambient-mark__copy small,
    .ambient-title span {
      font-size:
        4px;

      letter-spacing:
        1.1px;
    }


    .ambient-mark__copy strong,
    .ambient-title strong {
      font-size:
        6px;
    }


    .table-rules {
      bottom:
        10px;

      gap:
        6px;

      font-size:
        4px;

      letter-spacing:
        0.8px;
    }
  }


  /* ==========================================================
     SMALL SCREENS
     ========================================================== */

  @media (
    max-width: 520px
  ) {
    .ambient-mark {
      opacity:
        0.27;
    }


    .ambient-mark--right {
      display:
        none;
    }


    .ambient-title {
      display:
        none;
    }


    .table-rules {
      bottom:
        8px;

      opacity:
        0.18;

      gap:
        5px;

      font-size:
        4px;

      letter-spacing:
        0.65px;
    }


    .hud-vignette {
      background:
        radial-gradient(
          ellipse at center 46%,
          rgba(
            0,
            0,
            0,
            0
          )
          44%,
          rgba(
            0,
            0,
            0,
            0.018
          )
          62%,
          rgba(
            0,
            0,
            0,
            0.08
          )
          84%,
          rgba(
            0,
            0,
            0,
            0.18
          )
          100%
        );
    }
  }


  /* ==========================================================
     VERY SMALL SCREENS
     ========================================================== */

  @media (
    max-width: 380px
  ) {
    .ambient-mark--left {
      left:
        9px;
    }


    .ambient-mark {
      top:
        10px;
    }


    .ambient-mark__line {
      width:
        10px;
    }


    .table-rules {
      max-width:
        calc(
          100vw - 14px
        );

      gap:
        4px;
    }
  }


  /* ==========================================================
     REDUCED MOTION
     ========================================================== */

  @media (
    prefers-reduced-motion: reduce
  ) {
    .hud-vignette,
    .hud-grain {
      animation:
        none;
    }
  }
</style>